export default {
    currentArtistId(state) {
        return state.currentArtistId;
    },
    currentArtist(state) {
        return state.currentArtist;
    },
    artists(state) {
        return state.artists;
    }
}